{
  "name": "coordinator-webhook",
  "description": "Send coordination email via backend",
  "variables": {
    "BASE_URL": {
      "type": "string",
      "required": true
    },
    "SECRET": {
      "type": "string",
      "required": true
    }
  }
}
